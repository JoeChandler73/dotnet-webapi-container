{
  "id": "kubectl-k9s",
  "version": "1.0.0",
  "name": "kubectl and k9s",
  "description": "Installs kubectl and k9s for Kubernetes management",
  "options": {
    "kubectlVersion": {
      "type": "string",
      "default": "latest",
      "description": "Version of kubectl to install (e.g., 'v1.28.0' or 'latest')"
    },
    "k9sVersion": {
      "type": "string",
      "default": "latest",
      "description": "Version of k9s to install (e.g., 'v0.32.4' or 'latest')"
    },
    "installKubectx": {
      "type": "boolean",
      "default": true,
      "description": "Install kubectx and kubens utilities for context/namespace switching"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}